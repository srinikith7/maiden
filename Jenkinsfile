pipeline {
    agent any
    stages {
        stage('version') {
            steps {
                script {
                    sh pip install python3
                }
             }
         }
        
    }
}
